include utils

-
  const navInfo = data.navInfo;
  const title = data.title;
  const links = data.links;
//- -
//-     const title = "TEMPLATE_HOME"
//-     const links = {temp_page: "./template_page.html"}

+homePage(navInfo, title, links)